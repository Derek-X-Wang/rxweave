{
  "name": "rxweave",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22",
    "bun": ">=1.1"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.5.4",
    "@types/node": "^22.10.0",
    "vitest": "^2.1.0",
    "@effect/vitest": "^0.17.0",
    "oxlint": "^0.13.0"
  }
}
